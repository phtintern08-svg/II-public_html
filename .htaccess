# =========================================
# Root public_html .htaccess
# Purpose: Security + Static optimization ONLY
# DO NOT add routing logic here
# =========================================

# Disable directory listing
Options -Indexes

# -----------------------------------------
# Security headers (global, safe)
# -----------------------------------------
<IfModule mod_headers.c>
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

# -----------------------------------------
# Block sensitive files everywhere
# -----------------------------------------
<FilesMatch "\.(env|py|pyc|log|sql|md|bak|tmp)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# -----------------------------------------
# Allow static assets
# -----------------------------------------
<FilesMatch "\.(css|js|jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot|mp4|pdf|webp)$">
    Order allow,deny
    Allow from all
</FilesMatch>

# -----------------------------------------
# Compression
# -----------------------------------------
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE \
        text/html \
        text/plain \
        text/css \
        text/javascript \
        application/javascript \
        application/json \
        image/svg+xml
</IfModule>

# -----------------------------------------
# Cache static assets
# -----------------------------------------
<IfModule mod_expires.c>
    ExpiresActive On

    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/x-icon "access plus 1 year"

    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"

    ExpiresDefault "access plus 7 days"
</IfModule>

# -----------------------------------------
# Charset
# -----------------------------------------
AddDefaultCharset UTF-8
